# Enable Swagger UI in dev profile
springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true

# Gmail SMTP (dev credentials)
spring:
  mail:
    username: example@gmail.com
    password: paasword
